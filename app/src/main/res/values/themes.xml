<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TryIntente" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.TryIntente" parent="Base.Theme.TryIntente" />

    <style name="operatorBtn" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="backgroundTint">#d6d6fe

        </item>
        <item name="android:textColor">#3434fc</item>
        <item name="cornerRadius">200dp</item>
        <item name="android:layout_width">72dp</item>
        <item name="android:layout_height">72dp</item>
        <item name="android:layout_margin">12dp</item>
        <item name="android:textSize">30dp</item>

    </style>

    <style name="CommonButton"  parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="android:layout_width">72dp</item>
        <item name="android:layout_height">72dp</item>
        <item name="backgroundTint">@color/white</item>
        <item name="android:textColor">#3434fc</item>
        <item name="android:layout_margin">12dp</item>
        <item name="android:textSize">30dp</item>
        <item name="cornerRadius">200dp</item>
    </style>
</resources>